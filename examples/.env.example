# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=authkit
DB_SSLMODE=disable

# Server Configuration (optional, defaults from authkit config)
# SERVER_PORT=8080
# JWT_SECRET=your-secret-key-here
# JWT_EXPIRATION=24h
